specVersion: "3.0"
metadata:
  name: workflowmaker-ui5
type: application
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      mountPath: /odata/v2/device
      configuration:
        baseUri: "http://localhost:4004/odata/v2/device"
